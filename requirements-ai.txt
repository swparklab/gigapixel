# AI / learned feature-matching stack (LoFTR, LightGlue).
#
# NOTE: As of this update these packages are also listed in requirements.txt and
# install a CPU torch build by default. This file remains as the recommended
# path for an NVIDIA GPU, where you want a CUDA torch build instead of CPU.
#
# GPU install (example, CUDA 12.1) — install CUDA torch FIRST, then the rest:
#   py -3 -m pip install torch --index-url https://download.pytorch.org/whl/cu121
#   py -3 -m pip install -r requirements-ai.txt
# Then set STITCH_MATCHER_DEVICE=cuda (or leave it on auto).
#
# These power app/services/deep_matching.py. They are not strictly required:
# when absent, registration falls back to the classical SIFT/ORB pipeline.

torch>=2.2.0
kornia>=0.7.3
kornia-rs>=0.1.1

# Neural point-cloud -> 3D object mesh (highest quality, GPU only). NKSR is
# NVIDIA's Neural Kernel Surface Reconstruction — the SOTA learned surface
# reconstructor. Install AFTER a CUDA torch build. Without it, the pipeline
# uses Open3D screened-Poisson (see requirements.txt), which needs no GPU.
#   pip install nksr -f https://nksr.huyaoyu.com/whl/torch-2.2.0+cu121.html
# nksr>=1.0.3
